{
  "name": "intent-based-call-routing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Voice agent template: an inbound Teams Phone receptionist that classifies why someone is calling and transfers them to the right Teams destination with handoff context.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test test/*.test.mjs",
    "tunnel": "devtunnel host -p 8091 --allow-anonymous",
    "check": "for f in src/*.mjs src/voice/*.mjs; do node --check \"$f\" || exit 1; done; echo \"syntax ok\""
  },
  "dependencies": {
    "@azure/communication-call-automation": "^1.6.0",
    "@azure/identity": "^4.5.0",
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
